{
  "name": "blessing-ai",
  "version": "0.1.0",
  "description": "Blessing AI — assistant personnel chrétien de productivité (verset du jour, méditations, prières, tâches).",
  "private": true,
  "scripts": {
    "start": "npx serve www",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
